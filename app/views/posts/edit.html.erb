<h2>編集画面</h2>

<%= form_with model: @post do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :review_title %>

  <h4>ゲーム名</h4>
  <%= f.text_field :game_title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>
  
<% end %>
